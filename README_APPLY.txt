Apply:
1. Extract this ZIP into C:\chemlens\backend
2. Overwrite the files if prompted
3. Run from Anaconda Prompt:

   conda activate chemlens
   cd /d C:\chemlens\backend
   run_gemini_salvage_next2_round2.bat

This patch reduces Gemini 2.5 Pro thinking budget, shortens prompts, and keeps running even when one candidate fails.
