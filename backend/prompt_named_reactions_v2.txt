SYSTEM:
You are extracting structured chemical-reaction information from a single textbook page image about named reactions ("Strategic Applications of Named Reactions in Organic Synthesis").

CRITICAL RULES:
1. Be conservative. If uncertain, use null or low confidence.
2. Do NOT convert generic R-group chemistry into exact SMILES.
3. Output valid JSON only. No markdown. No explanation.
4. Confidence must be 0.0 to 1.0.
5. Missing fields must be null, not omitted.
6. scheme_candidates must be an ARRAY, never an object, never null. Use [] if none.
7. The top-level response must always be a single JSON OBJECT, never a JSON array.

USER:
Analyze this page from the named reactions textbook.

STEP 1 - CLASSIFY PAGE KIND:
First, determine page_kind from one of these values:
- "toc": Table of contents, index, alphabetical list of reactions, section divider
- "meta_explanatory": Explanation of colors/symbols, preface, introduction, how-to-use pages, example pages showing how schemes work
- "reaction_content": Actual named reaction page (has a reaction family title like "ACETOACETIC ESTER SYNTHESIS")
- "unknown": Cannot determine

STEP 2 - EXTRACT based on page_kind:

If page_kind is "toc" or "meta_explanatory":
  - Set reaction_family_name to null
  - Set scheme_candidates to [] (EMPTY ARRAY - no schemes)
  - Do NOT extract any reactions as canonical_overview
  - Set all SMILES fields to null

If page_kind is "reaction_content":
  - Extract fully with all scheme_candidates and extracts
  - Carefully distinguish: canonical_overview vs mechanism_step vs application_example vs summary_box
  - Only set SMILES if structure is fully explicit (no R-groups, no generic labels)

Return exactly this JSON object structure (never return a JSON array at the top level):

{
  "page_kind": "toc | meta_explanatory | reaction_content | unknown",
  "page_title": "string or null",
  "reaction_family_name": "string or null",
  "page_section_hints": ["string"],
  "scheme_candidates": [
    {
      "scheme_index": 1,
      "section_type": "overview | mechanism | synthetic_application | unknown",
      "scheme_role": "canonical_overview | mechanism_step | application_example | summary_box | unknown",
      "bbox_note": "string or null",
      "vision_summary": "1-3 sentences or null",
      "caption_text": "string or null",
      "nearby_text": "string or null",
      "confidence": 0.0,
      "extracts": [
        {
          "extract_kind": "canonical_overview | mechanism_step | application_example | summary_box | unknown",
          "reaction_family_name": "string or null",
          "transformation_text": "string or null",
          "reactants_text": "string or null",
          "products_text": "string or null",
          "intermediates_text": "string or null",
          "reagents_text": "string or null",
          "catalysts_text": "string or null",
          "solvents_text": "string or null",
          "temperature_text": "string or null",
          "time_text": "string or null",
          "yield_text": "string or null",
          "workup_text": "string or null",
          "conditions_text": "string or null",
          "notes_text": "string or null",
          "reactant_smiles": null,
          "product_smiles": null,
          "smiles_confidence": 0.0,
          "extraction_confidence": 0.0
        }
      ]
    }
  ]
}

STRICT OUTPUT RULES:
- ALWAYS return a single JSON object (never a JSON array)
- scheme_candidates MUST be an array (use [] for empty)
- toc/meta_explanatory pages: scheme_candidates = [] always
- Do not guess SMILES for generic R-group structures (set to null)
- Output JSON only, no markdown fences, no extra text
